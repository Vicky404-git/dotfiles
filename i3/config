# --- i3 CONFIGURATION (Translated from Hyprland) ---

# Set Windows/Super key as the main modifier
set $mod Mod4

# Font for window titles and bar
font pango:monospace 10

exec --no-startup-id /home/vicky-404/Desktop/Projects/aalas-ka-hit/app.sh
for_window [title="aalas-ka-hit.*"] floating enable

default_border pixel 2
default_floating_border pixel 2

# --- Autostart Applications ---
# Start picom for blur, shadows, and animations
exec --no-startup-id picom -b
# Start bluetooth 
exec --no-startup-id blueman-applet
# Start network manager applet
exec --no-startup-id nm-applet
# Set your wallpaper (Replace the path with your actual anime wallpaper image)
exec_always --no-startup-id feh --bg-fill ~/dotfiles/wallpaper/wallhaven1.png
# Start the notification daemon
exec --no-startup-id dunst

exec --no-startup-id kdeconnect-indicator

# Start DaemonV Background Observer
exec --no-startup-id bash -c "rm -f ~/.daemonv.lock; java -jar ~/.daemonv/daemonv.jar"

# Launch btop in a dedicated terminal on boot
exec --no-startup-id kitty -T "SystemMonitor" btop --utf-force
for_window [title="SystemMonitor"] border pixel 0

# --- Floating Lyrics ---
# Force it to float, center it, remove borders, and set a clean size
for_window [class="floating_lyrics"] floating enable, move position center, resize set 800 600, border pixel 0

# Toggle Music HUD (Glava + Lyrics)
bindsym $mod+m exec kitty --class floating_lyrics -e sptlrx

# --- Visuals (Gaps & Borders) ---
gaps inner 5
gaps outer 10
default_border pixel 2
default_floating_border pixel 2

# --- Core Keybindings ---
# Terminal
bindsym $mod+Return exec kitty

# Lock the screen
bindsym $mod+x exec --no-startup-id ~/.config/i3/lock.sh

# App Launcher
bindsym $mod+d exec "rofi -show drun"

# Close Window
bindsym $mod+q kill

# --- Night Mode (Redshift) ---
# Turn night mode on (Adjust 4500 to your preferred temperature)
bindsym $mod+n exec --no-startup-id redshift -O 4500

# Turn night mode off
bindsym $mod+Shift+n exec --no-startup-id redshift -x

# Reload config (Hyprland was Mod+Shift+R, i3 usually uses Mod+Shift+C to reload and R to restart)
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# Floating Toggle
bindsym $mod+v floating toggle

# Launch Yazi File Manager
bindsym $mod+e exec kitty -e yazi

# --- Focus Movement (Vim keys) ---
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# Move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# --- Tiling Modifiers ---
# Split horizontal
bindsym $mod+b split h
# Split vertical
bindsym $mod+Shift+v split v
# Fullscreen
bindsym $mod+f fullscreen toggle

# --- The Scratchpad ---
# Move window to scratchpad
bindsym $mod+Shift+s move scratchpad
# Toggle scratchpad visibility
bindsym $mod+s scratchpad show

# --- Workspaces ---
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws0 "0"

# --- Hardware Dashboard ---
# Force the system monitor to always open on Workspace V
assign [title="SystemMonitor"] "5"

# --- DYNAMIC THEME WORKSPACES ---
# When switching workspaces, feh instantly applies the corresponding wallpaper
bindsym $mod+1 workspace 1; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/1.jpg
bindsym $mod+2 workspace 2; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/2.jpg
bindsym $mod+3 workspace 3; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/3.jpg
bindsym $mod+4 workspace 4; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/4.jpg
bindsym $mod+5 workspace 5; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/5.jpg

# Optional: Add 6-10 if you use them
bindsym $mod+6 workspace 6; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/1.jpg
bindsym $mod+7 workspace 7; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/2.jpg
bindsym $mod+8 workspace 8; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/3.jpg
bindsym $mod+9 workspace 9; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/4.jpg
bindsym $mod+0 workspace 0; exec --no-startup-id feh --bg-fill ~/.config/wallpapers/current/5.jpg



bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws0


# --- Screenshots  ---
# Screenshot
bindsym Print exec --no-startup-id flameshot gui

# --- Screenrecord -- 
# Launch OBS Studio
bindsym $mod+o exec obs

# --- Hardware Controls ---
bindsym XF86AudioRaiseVolume exec --no-startup-id wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindsym XF86AudioLowerVolume exec --no-startup-id wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindsym XF86AudioMute exec --no-startup-id wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set 5%+
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-

# --- Media Playback Controls ---
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioStop exec playerctl stop
bindsym XF86AudioPrev exec playerctl previous
bindsym XF86AudioNext exec playerctl next

# --- Exit i3 ---
# Custom Rofi Power Menu
bindsym $mod+Shift+e exec --no-startup-id ~/.config/i3/powermenu.sh
# Launch Theme Switcher Menu
bindsym $mod+t exec ~/.config/i3/rofi_theme.sh
# --- Status Bar ---
exec_always --no-startup-id ~/.config/polybar/launch.sh

# --- Drop-down Scratchpad Terminal ---
# 1. Launch a specifically named Kitty window on boot
exec --no-startup-id kitty --class scratchpad_term

# 2. Force it to float, set a good size, and banish it to the hidden scratchpad
for_window [class="scratchpad_term"] floating enable, resize set 1000 650, move scratchpad, border pixel 2

# 3. Bind Super + ` (the tilde key above Tab) to summon and dismiss it
bindsym $mod+grave [class="scratchpad_term"] scratchpad show; move position center

# Move any currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# Cycle through all hidden scratchpad windows
bindsym $mod+minus scratchpad show
